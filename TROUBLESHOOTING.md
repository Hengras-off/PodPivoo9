# 🔧 Решение проблемы: Открывается не тот фильм

## Проблема
При нажатии "Смотреть онлайн" открывается не тот фильм/сериал, который вы выбрали.

## Причины
1. **Неправильный IMDB ID** - база TMDB может содержать устаревший или неправильный ID
2. **Плеер не поддерживает данный контент** - не все фильмы есть в каждой базе
3. **Путаница movie/tv** - неправильный mediaType
4. **Kinopoisk ID отсутствует** - русские плееры лучше работают с KP ID

## Решения

### 1. Проверьте ID в консоли браузера

Откройте консоль (F12) и посмотрите что выводится:
```
RussianPlayer IDs: {
  imdbId: "tt1234567",
  tmdbId: "12345",
  kinopoiskId: null,
  mediaType: "movie",
  title: "Название фильма"
}
Current player: Kodik
Embed URL: https://kodik.info/find-player?imdb_id=tt1234567
```

### 2. Попробуйте разные плееры

Если один плеер показывает не тот фильм:
- ✅ **Переключитесь** на другую озвучку (вкладки вверху)
- ✅ Попробуйте **HDVB** или **Collaps** - они более точные
- ✅ **Kodik** имеет самую большую базу, но иногда путает похожие фильмы

### 3. Проверьте ID на IMDB

В правом нижнем углу плеера показан **IMDB ID**.
1. Кликните на ссылку "IMDB" или Кинопоиск
2. Проверьте что открылся правильный фильм
3. Если нет - значит TMDB предоставил неправильный ID

### 4. Используйте поиск на плеерах

Некоторые плееры позволяют искать внутри:
- **Kodik** - можно искать по названию
- **Filmix** - поиск по базе
- **Alloha** - для аниме по названию

## Какой плеер лучше для точности?

### Самые точные (по опыту):
1. **HDVB** 🥇 - Лучшая точность соответствия
2. **Collaps** 🥈 - Хорошая точность
3. **VideoCDN** 🥉 - CDN с проверкой

### Самая большая база:
1. **Kodik** - огромная база, но иногда путает
2. **Filmix** - много старых фильмов
3. **Bazon** - средняя точность

### Для аниме:
1. **Alloha** - специализируется на аниме, высокая точность

## Временное решение

Если у вас постоянно открывается не тот контент:

### Вариант 1: Использовать TMDB ID вместо IMDB

Обновите MovieDetailPage.js:
```javascript
{showPlayer && (
  <RussianPlayer
    imdbId={null}  // Отключить IMDB
    tmdbId={id}    // Использовать только TMDB
    // ...
  />
)}
```

### Вариант 2: Добавить приоритет плееров

В RussianPlayer.js измените порядок источников:
```javascript
// Сначала самые точные:
const sources = [
  { name: 'HDVB', ... },      // Самый точный
  { name: 'Collaps', ... },   // Тоже точный
  { name: 'VideoCDN', ... },
  { name: 'Kodik', ... },     // Последним, т.к. большая база
  // ...
];
```

### Вариант 3: Добавить поиск по названию

Для некоторых плееров можно использовать название + год:
```javascript
{
  name: 'Kodik Search',
  getUrl: () => {
    const searchQuery = encodeURIComponent(`${title} ${year}`);
    return `https://kodik.info/search?q=${searchQuery}`;
  }
}
```

## Отладка

### Шаг 1: Проверьте какой ID передается
Откройте F12 → Console, найдите:
```
RussianPlayer IDs: { ... }
```

### Шаг 2: Проверьте сформированный URL
Найдите в консоли:
```
Embed URL: https://...
```

### Шаг 3: Откройте URL в новой вкладке
Скопируйте URL из консоли и откройте отдельно - проверьте что показывает плеер

### Шаг 4: Сравните с оригиналом
- Откройте IMDB/Кинопоиск по ссылке из плеера
- Сравните с тем фильмом что вы выбрали
- Если разные - проблема в TMDB базе данных

## Постоянное решение

### Добавить проверку перед открытием

Можно добавить предпросмотр перед полноэкранным плеером:

```javascript
// В MovieDetailPage.js
const [playerInfo, setPlayerInfo] = useState(null);

// Показать превью с информацией
<div className="bg-card p-4 rounded-lg mb-4">
  <h3>Проверьте что это правильный фильм:</h3>
  <p>IMDB: {details.external_ids?.imdb_id}</p>
  <p>Название: {details.title}</p>
  <p>Год: {year}</p>
  <button onClick={() => setShowPlayer(true)}>
    Подтвердить и смотреть
  </button>
</div>
```

## Сообщить о проблеме

Если определенный фильм всегда открывается неправильно:
1. Запишите **название фильма**
2. Запишите **IMDB ID** из консоли
3. Запишите **что открылось** вместо этого
4. Какой **плеер** использовался
5. Создайте Issue на GitHub

## FAQ

**Q: Почему некоторые плееры точнее других?**
A: Разные плееры используют разные базы данных. HDVB и Collaps имеют более точное сопоставление ID.

**Q: Можно ли использовать только один точный плеер?**
A: Да, можно оставить только HDVB или Collaps в списке источников.

**Q: Почему для аниме открывается не та серия?**  
A: Многие плееры путают сезоны. Используйте **Alloha** специально для аниме.

**Q: Что делать если ID вообще нет?**
A: Некоторые фильмы могут не иметь IMDB ID в базе TMDB. Попробуйте найти вручную на Кинопоиске.

---

## Быстрый чеклист

- [ ] Откройте консоль (F12) и проверьте ID
- [ ] Попробуйте 2-3 разных плеера
- [ ] Используйте HDVB для максимальной точности
- [ ] Проверьте IMDB ID кликнув на ссылку
- [ ] Для аниме используйте Alloha
- [ ] Сообщите о постоянной проблеме

**Если ничего не помогает** - возможно фильм просто отсутствует в русских базах. Попробуйте англоязычные плееры из MultiSourcePlayer.
